{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug catalogo_run",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/etapa1_c/build/catalogo_run",
            "args": ["${workspaceFolder}/etapa1_c/ArchivosEntrada/info_cursos/ingComputadores/catalogo_cursos_computadores.json"],
            "cwd": "${workspaceFolder}",
            "MIMode": "gdb",
            "preLaunchTask": "C/C++: gcc build catalogo (main + modulos + cJSON)"
        }
    ]
}